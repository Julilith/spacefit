<%= form_for Feedback.new, class: "form-group"  do |f| %>
	<label class="control-label">Ci-dessous donnez nous anonymement votre avis, vos suggestions, vos questions....</label>
	<%= f.text_area :text, class: "form-control", rows: 8 %>
	<button type="submit" class="btn btn-primary full-width m-t">Envoyer</button>
<% end %>
